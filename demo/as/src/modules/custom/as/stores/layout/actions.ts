export default {
  showLeft: (show: boolean): boolean => {
    return show
  },
  showHead: (show: boolean): boolean => {
    return show
  },
  setLayout: (mode: string): string => {
    return mode
  }
}
